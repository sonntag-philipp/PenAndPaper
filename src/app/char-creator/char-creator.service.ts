import { CharacterModel } from '../shared/character.model';
import { NgForm } from '@angular/forms';

export class CharCreatorService {
  Character: CharacterModel;

  BaseForm: NgForm;

  SaveCharacter(){

  }
}